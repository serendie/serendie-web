---
title: Reference Tokens
slug: foundations/design-tokens/reference-tokens
parent: foundations/design-tokens
illustType: "A"
description: "リファレンストークンは、カラーコードなど生の値を参照するトークンであり、このトークンをプロダクトのUI内で使用することは例外的です。"
lastUpdated: "2024-10-23"
---

import { TokenList } from "src/components/TokenList";
import tokens from "@serendie/design-token/token-list";
import { PageSection } from "../../../components/PageLayout.tsx";

export const referenceTokens = tokens.filter((token) =>
  token.key.includes("sd.reference")
);

<PageSection>
  TODO: 右サイドバー部分を消す
  <TokenList tokens={referenceTokens} client:load />
</PageSection>
