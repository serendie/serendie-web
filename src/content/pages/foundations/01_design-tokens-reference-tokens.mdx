---
title: リファレンストークン
slug: foundations/design-tokens/reference-tokens
parent: foundations/design-tokens
---

import { TokenList } from "src/components/TokenList";
import tokens from "@serendie/design-token/token-list";
import { links } from "./01_design-tokens.mdx";

import { IndexPageHeader } from "../../../components/IndexPageHeader.tsx";
import {
  PageMain,
  PageSection,
  PageAside,
  PageLinks,
  PageA,
  PageARef,
} from "../../../components/PageLayout.tsx";
import PageHeader from "@/components/PageHeader.astro";

export const referenceTokens = tokens.filter((token) =>
  token.key.includes("sd.reference")
);

<PageHeader links={links}>
  <IndexPageHeader
    illustType="A"
    title={"Design Tokens"}
    description={
      "デザイントークンはデザインシステムを構成する最小単位です。デザインと開発でデザイントークンを共有することで、Single Source of Truthを実現します。"
    }
  />
</PageHeader>
<PageMain gridColumn={"span 8"}>
  <PageSection>
    <TokenList tokens={referenceTokens} client:load />
  </PageSection>
</PageMain>
