---
title: データビジュアライゼーション - カラーパレット
slug: foundations/data-visualization-palette
lastUpdated: "2025-06-19"
illustType: A
description: データビジュアライゼーション専用のカラーパレットです。チャートやグラフで使用する色の一覧と使用方法を示します。
parent: foundations/data-visualization
subLinks:
  [
    { name: "カラーの使い方", path: "/foundations/data-visualization" },
    { name: "カラーパレット", path: "/foundations/data-visualization-palette" },
  ]
---

import PageHeadLine from "../../../components/PageHeadLine.astro";
import {
  PageMain,
  PageSection,
  PageAside,
  PageLinks,
  PageA,
} from "../../../components/PageLayout.tsx";
import StickySideNav from "../../../components/StickySideNav.astro";
import { css } from "styled-system/css";
import { CalloutSection } from "../../../components/CalloutSection.tsx";
import { ChartColorMatrix } from "@/components/ChartColorMatrix.tsx";
import { chartColors } from "@/data/chartColors.ts";

<PageSection>
  <PageHeadLine number={1}>Mark</PageHeadLine>

## Single（単色色相）

チャート本体に使用する単色色相のカラーパレットです。データ数が３以下の場合は偶数のカラーを使用してください。

<div
  className={css({
    fontFamily: "sd.reference.typography.fontFamily.monospace",
    fontSize: "12px",
    color: "sd.reference.color.scale.gray.1000",
    marginBottom: "16px",
    marginTop: "24px",
    letterSpacing: "0.6px",
  })}
>
  sd.system.color.chart.mark.primary
</div>

<div
  className={css({
    mt: "sd.system.dimension.spacing.twoExtraLarge",
    mb: "sd.system.dimension.spacing.medium",
    overflowX: "visible",
    overflowY: "visible",
    position: "relative",
    px: "sd.system.dimension.spacing.medium",
    mx: "-sd.system.dimension.spacing.medium",
  })}
>
  <ChartColorMatrix data={chartColors.single} client:idle />
</div>

## Semantic

成功やエラー、注意など、データに特定の意味や目的を持たせる場合はSemanticカラーパレットを使用してください。データ数が入れ子の場合は階層別にカラーを使用してください。

<div
  className={css({
    fontFamily: "sd.reference.typography.fontFamily.monospace",
    fontSize: "12px",
    color: "sd.reference.color.scale.gray.1000",
    marginBottom: "16px",
    marginTop: "24px",
    letterSpacing: "0.6px",
  })}
>
  sd.system.color.chart.mark
</div>

<div
  className={css({
    mt: "sd.system.dimension.spacing.twoExtraLarge",
    mb: "sd.system.dimension.spacing.medium",
    overflowX: "visible",
    overflowY: "visible",
    position: "relative",
    px: "sd.system.dimension.spacing.medium",
    mx: "-sd.system.dimension.spacing.medium",
  })}
>
  <ChartColorMatrix data={chartColors.semantic} client:idle />
</div>

## Multi（複数色相）

チャート本体に使用する複数色相のカラーパレットです。各色を個別に分類が認識できるように、パレットの番号の順番に従って使用してください。

<div
  className={css({
    fontFamily: "sd.reference.typography.fontFamily.monospace",
    fontSize: "12px",
    color: "sd.reference.color.scale.gray.1000",
    marginBottom: "16px",
    marginTop: "24px",
    letterSpacing: "0.6px",
  })}
>
  sd.system.color.chart.mark.multi
</div>

<div
  className={css({
    mt: "sd.system.dimension.spacing.twoExtraLarge",
    mb: "sd.system.dimension.spacing.medium",
    overflowX: "visible",
    overflowY: "visible",
    position: "relative",
    px: "sd.system.dimension.spacing.medium",
    mx: "-sd.system.dimension.spacing.medium",
  })}
>
  <ChartColorMatrix data={chartColors.multi} client:idle />
</div>

</PageSection>

<PageSection>
  <PageHeadLine number={2}>Component</PageHeadLine>

チャート内での目盛やラベルなど、マーク（チャート本体）以外の要素はこちらのパレットを使用してください。

<div
  className={css({
    fontFamily: "sd.reference.typography.fontFamily.monospace",
    fontSize: "12px",
    color: "sd.reference.color.scale.gray.1000",
    marginBottom: "16px",
    marginTop: "24px",
    letterSpacing: "0.6px",
  })}
>
  sd.system.color.chart.component
</div>

<div
  className={css({
    mt: "sd.system.dimension.spacing.twoExtraLarge",
    mb: "sd.system.dimension.spacing.medium",
    overflowX: "visible",
    overflowY: "visible",
    position: "relative",
    px: "sd.system.dimension.spacing.medium",
    mx: "-sd.system.dimension.spacing.medium",
  })}
>
  <ChartColorMatrix data={chartColors.component} client:idle />
</div>

</PageSection>
