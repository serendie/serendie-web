---
slug: foundations/design-tokens/system-tokens
parent: foundations/design-tokens
title: System Tokens
illustType: "A"
description: "システムトークンは、リファレンストークンを参照するトークンです。UIをとりまくコンテキストが変化した際には、その参照先を切り替えることで対応します。"
lastUpdated: "2024-11-1"
fullWidth: true
subLinks:
  [
    { name: "設計", path: "/foundations/design-tokens" },
    {
      name: "リファレンストークン",
      path: "/foundations/design-tokens/reference-tokens",
    },
    {
      name: "システムトークン",
      path: "/foundations/design-tokens/system-tokens",
    },
  ]
---

import { TokenList } from "src/components/TokenList";
import tokens from "@serendie/design-token/token-list";
import { PageSection } from "../../../components/PageLayout.tsx";
import PageHeader from "@/components/PageHeader.astro";

export const systemTokens = tokens.filter(
  (token) => !token.key.includes("themes") && token.key.includes("sd.system")
);

<PageSection>
  <TokenList tokens={systemTokens} client:load />
</PageSection>
